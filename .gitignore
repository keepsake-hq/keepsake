/target
**/target/
**/*.rs.bk
Cargo.lock.bak
.DS_Store

# Local model cache (downloaded at runtime; never commit the model)
.fastembed_cache/
**/.fastembed_cache/

# Node / Tailwind build tooling
**/node_modules/

# Build artifacts
*.dmg

# Signing keys — NEVER commit. The updater private key lives outside the repo (~/.keepsake/).
*.key
*.key.pub
